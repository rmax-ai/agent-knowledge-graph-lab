// @agkl/compiler — Deterministic knowledge compilation pipeline
// parse → validate → normalize → resolve → diagnose → emit

export { compileCorpus } from "./compiler";
export type { CompileOptions } from "./compiler";
